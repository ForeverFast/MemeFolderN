<UserControl x:Class="MemeFolderN.DevTools.Views.DevBoard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MemeFolderN.DevTools.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">

            <mfbuttonuc:MFButton x:Name="GcButton"
                                 TextData="GC"
                                 WindowChrome.IsHitTestVisibleInChrome="True"
                                 Command="{Binding ShowExecutableMethodCommand}"
                                 Background="{StaticResource GrayLVL4}"/>
        </StackPanel>
    </Grid>
</UserControl>
